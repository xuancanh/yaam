{
  "manifest": 3,
  "name": "GitHub Issues",
  "version": "2.1.1",
  "icon": "🐙",
  "description": "A GitHub issue triage inbox — sync open issues from any number of repos on a schedule, spawn linked board tasks (or let the addon's customizable triage agent decide), track their progress, and optionally close the issue on GitHub when the task is done. Token stays in the OS keychain.",
  "author": "yaam",
  "hosts": [
    "api.github.com"
  ],
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "label": "GitHub personal access token (repo scope; optional for public repos, required to close issues)"
    }
  ],
  "permissions": [
    "state:read",
    "tasks",
    "schedules",
    "storage",
    "ui",
    "http",
    "secrets",
    "agent"
  ],
  "hooks": {
    "onCronFired": "const module = { exports: {} };\nconst exports = module.exports;\nvar __defProp = Object.defineProperty;\nvar __getOwnPropDesc = Object.getOwnPropertyDescriptor;\nvar __getOwnPropNames = Object.getOwnPropertyNames;\nvar __hasOwnProp = Object.prototype.hasOwnProperty;\nvar __export = (target, all) => {\n  for (var name in all)\n    __defProp(target, name, { get: all[name], enumerable: true });\n};\nvar __copyProps = (to, from, except, desc) => {\n  if (from && typeof from === \"object\" || typeof from === \"function\") {\n    for (let key of __getOwnPropNames(from))\n      if (!__hasOwnProp.call(to, key) && key !== except)\n        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });\n  }\n  return to;\n};\nvar __toCommonJS = (mod) => __copyProps(__defProp({}, \"__esModule\", { value: true }), mod);\n\n// registry/src/github-issues/src/hooks/onCronFired.ts\nvar onCronFired_exports = {};\n__export(onCronFired_exports, {\n  default: () => onCronFired_default\n});\nmodule.exports = __toCommonJS(onCronFired_exports);\nvar handler = async (input, api) => {\n  if (input.name !== \"github-issues-sync\") return;\n  const cfg = await api.storage.get(\"config\") || {};\n  if (!Array.isArray(cfg.repos)) {\n    cfg.repos = cfg.owner && cfg.repo ? [{ name: cfg.owner + \"/\" + cfg.repo, on: true }] : [];\n    cfg.legacy = cfg.owner && cfg.repo ? cfg.owner + \"/\" + cfg.repo : \"\";\n    cfg.freq = cfg.schedOn ? cfg.interval || \"*/30 * * * *\" : \"manual\";\n    await api.storage.set(\"config\", cfg);\n  }\n  const repos = cfg.repos.filter((r) => r.on);\n  if (!repos.length) return;\n  let issues = await api.storage.get(\"issues\") || [];\n  const queue = await api.storage.get(\"queue\") || [];\n  if (queue.length) {\n    const repo = cfg.legacy || repos[0] && repos[0].name || \"\";\n    for (const q of queue) {\n      const key = repo + \"#\" + q.number;\n      if (!issues.some((i) => i.key === key)) {\n        issues.push({\n          key,\n          number: q.number,\n          repo,\n          title: q.title,\n          body: q.body || \"\",\n          labels: q.labels || [],\n          url: q.url,\n          author: \"\",\n          comments: 0,\n          createdAt: q.at,\n          at: q.at,\n          state: \"inbox\",\n          taskId: null\n        });\n      }\n    }\n    await api.storage.remove(\"queue\");\n  }\n  const secrets = await api.secrets.list();\n  const headers = { accept: \"application/vnd.github+json\" };\n  if (secrets.some((s) => s.name === \"GITHUB_TOKEN\" && s.set)) headers.authorization = \"Bearer {{secret:GITHUB_TOKEN}}\";\n  const seen = await api.storage.get(\"seen\") || [];\n  const labels = cfg.labels ? \"&labels=\" + encodeURIComponent(cfg.labels) : \"\";\n  const fresh = [];\n  for (const r of repos) {\n    const res = await api.http.request(\n      \"GET\",\n      `https://api.github.com/repos/${r.name}/issues?state=open&per_page=50&sort=created&direction=desc${labels}`,\n      { headers }\n    );\n    if (res.status !== 200) {\n      await api.logEvent(`GitHub sync failed for ${r.name}: HTTP ${res.status}`);\n      continue;\n    }\n    for (const raw of JSON.parse(res.text)) {\n      if (raw.pull_request) continue;\n      const key = r.name + \"#\" + raw.number;\n      const ex = issues.find((i) => i.key === key);\n      if (ex) {\n        ex.title = raw.title;\n        ex.comments = raw.comments || 0;\n        ex.labels = (raw.labels || []).map((l) => l.name);\n        continue;\n      }\n      if (r.name === cfg.legacy && seen.includes(raw.number)) continue;\n      const rec = {\n        key,\n        number: raw.number,\n        repo: r.name,\n        title: raw.title,\n        body: (raw.body || \"\").slice(0, 4e3),\n        labels: (raw.labels || []).map((l) => l.name),\n        url: raw.html_url,\n        author: raw.user && raw.user.login || \"\",\n        comments: raw.comments || 0,\n        createdAt: Date.parse(raw.created_at) || Date.now(),\n        at: Date.now(),\n        state: \"inbox\",\n        taskId: null\n      };\n      issues.push(rec);\n      fresh.push(rec);\n    }\n  }\n  await api.storage.set(\"issues\", issues.slice(-300));\n  await api.storage.set(\"lastSync\", { at: Date.now(), found: fresh.length });\n  if (!fresh.length) return;\n  if (cfg.auto) {\n    await api.agent.wake(\"New GitHub issues arrived \\u2014 triage them per your instructions:\\n\\n\" + JSON.stringify(fresh.map((f) => ({ key: f.key, number: f.number, repo: f.repo, title: f.title, labels: f.labels, body: (f.body || \"\").slice(0, 400) })), null, 2));\n  } else {\n    await api.notify(\"GitHub Issues\", `${fresh.length} new issue(s) in the inbox \\u2014 review them in the GitHub Issues tab`);\n  }\n};\nvar onCronFired_default = handler;\nconst __handler = typeof module.exports === \"function\" ? module.exports : module.exports.default;\nif (typeof __handler !== \"function\") throw new Error(\"handler module needs a default export function (input, api) => ...\");\nreturn await __handler(input, api);",
    "onTaskMoved": "const module = { exports: {} };\nconst exports = module.exports;\nvar __defProp = Object.defineProperty;\nvar __getOwnPropDesc = Object.getOwnPropertyDescriptor;\nvar __getOwnPropNames = Object.getOwnPropertyNames;\nvar __hasOwnProp = Object.prototype.hasOwnProperty;\nvar __export = (target, all) => {\n  for (var name in all)\n    __defProp(target, name, { get: all[name], enumerable: true });\n};\nvar __copyProps = (to, from, except, desc) => {\n  if (from && typeof from === \"object\" || typeof from === \"function\") {\n    for (let key of __getOwnPropNames(from))\n      if (!__hasOwnProp.call(to, key) && key !== except)\n        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });\n  }\n  return to;\n};\nvar __toCommonJS = (mod) => __copyProps(__defProp({}, \"__esModule\", { value: true }), mod);\n\n// registry/src/github-issues/src/hooks/onTaskMoved.ts\nvar onTaskMoved_exports = {};\n__export(onTaskMoved_exports, {\n  default: () => onTaskMoved_default\n});\nmodule.exports = __toCommonJS(onTaskMoved_exports);\nvar handler = async (input, api) => {\n  if (input.col !== \"done\") return;\n  const cfg = await api.storage.get(\"config\") || {};\n  if (!cfg.autoClose) return;\n  const issues = await api.storage.get(\"issues\") || [];\n  const iss = issues.find((i) => i.state === \"synced\" && i.taskId === input.taskId && !i.closed);\n  if (!iss) return;\n  const secrets = await api.secrets.list();\n  if (!secrets.some((s) => s.name === \"GITHUB_TOKEN\" && s.set)) {\n    await api.logEvent(`GitHub Issues: cannot close #${iss.number} \\u2014 no GITHUB_TOKEN stored`);\n    return;\n  }\n  const headers = {\n    accept: \"application/vnd.github+json\",\n    authorization: \"Bearer {{secret:GITHUB_TOKEN}}\",\n    \"content-type\": \"application/json\"\n  };\n  await api.http.request(\n    \"POST\",\n    `https://api.github.com/repos/${iss.repo}/issues/${iss.number}/comments`,\n    { headers, body: JSON.stringify({ body: `Resolved by the linked yaam board task: \"${String(input.title).slice(0, 120)}\".` }) }\n  ).catch(() => {\n  });\n  const res = await api.http.request(\n    \"PATCH\",\n    `https://api.github.com/repos/${iss.repo}/issues/${iss.number}`,\n    { headers, body: JSON.stringify({ state: \"closed\" }) }\n  );\n  if (res.status === 200) {\n    iss.closed = true;\n    await api.storage.set(\"issues\", issues);\n    await api.notify(\"GitHub Issues\", `Closed ${iss.repo}#${iss.number} \\u2014 its task reached Done`);\n  } else {\n    await api.logEvent(`GitHub Issues: closing #${iss.number} failed \\u2014 HTTP ${res.status}`);\n  }\n};\nvar onTaskMoved_default = handler;\nconst __handler = typeof module.exports === \"function\" ? module.exports : module.exports.default;\nif (typeof __handler !== \"function\") throw new Error(\"handler module needs a default export function (input, api) => ...\");\nreturn await __handler(input, api);"
  },
  "agent": {
    "system": "You are the triage officer for a GitHub-issues → task-board pipeline. When woken with a batch of new issues (JSON, each with a `key` like `owner/repo#123`), decide for each one whether it becomes a board task.\n\nDefault policy (the user can refine this in the addon's Customize chat):\n- File a task for concrete, actionable issues: bugs with reproduction details, small well-scoped features, chores.\n- Skip questions, discussions, duplicates of queued/board work (check get_state first), and anything labelled `wontfix`, `duplicate`, or `question`.\n\nIssue records live in the storage key `issues`: an array of objects with `key`, `number`, `repo`, `title`, `body`, `labels`, `url`, `state` (`inbox` | `synced` | `ignored` | `archived`), and `taskId`.\n\nFor each issue you accept:\n1. Create the task with add_task — title `#<number> · <issue title>`, the issue body (plus its URL) as the description, and 1-3 verifiable acceptance criteria you derive from the issue. Use the `cwd` from storage key `config` (field `cwd`) when set. Do NOT start tasks yourself unless storage `config.autostart` is true.\n2. In storage `issues`, find the record by `key`, set `state` to `\"synced\"` and `taskId` to the new task's id, then save the whole array back.\n\nFor each issue you skip: set its record's `state` to `\"ignored\"`, and keep a one-line reason in storage `triageLog` (array, newest first, cap 50).\n\nFinish with a short summary: how many filed, how many skipped, why.\n"
  },
  "html": "<!DOCTYPE html><html><head><meta charset=\"utf-8\"><style>\n:root{--bg:#0A0B0F;--bg2:#0B0C10;--panel:#12151C;--card:#0D0F14;--line:#1a1e26;--line2:#23272F;--line3:#2C313B;--text:#E7E9F0;--soft:#C7CCD6;--mut:#8B93A1;--mut2:#9AA3B2;--dim:#626B79;--acc:#F5C451;--accsoft:rgba(245,196,81,.13);--accbord:rgba(245,196,81,.4);--green:#3DDC97;--red:#FF5C5C;--redsoft:#FF7A7A;--amber:#FFB020;--purple:#A371F7}\n*{box-sizing:border-box}\nbody{margin:0;background:var(--bg);color:var(--text);font-family:'IBM Plex Sans',system-ui,sans-serif;font-size:13px;display:flex;flex-direction:column;height:100vh;overflow:hidden}\n.mono{font-family:'JetBrains Mono',monospace}\nbutton{font-family:inherit;cursor:pointer}\nbutton:disabled{opacity:.45;cursor:default;pointer-events:none}\ninput{font-family:inherit;outline:none}\n\n#topbar{height:46px;flex:none;background:linear-gradient(180deg,#13161E,#0C0E13);border-bottom:1px solid var(--line2);display:flex;align-items:center;gap:12px;padding:0 18px}\n#topbar .title{font-weight:600;font-size:14px}\n.seg{display:flex;align-items:center;gap:3px;background:var(--panel);border:1px solid var(--line2);border-radius:9px;padding:3px}\n.seg button{border:1px solid transparent;background:transparent;color:var(--mut);font-size:12px;font-weight:600;padding:4px 12px;border-radius:7px}\n.seg button:hover{color:var(--soft)}\n.seg button.on{background:var(--accsoft);color:var(--acc);border-color:var(--accbord)}\n.btn{display:flex;align-items:center;gap:7px;background:#171B23;border:1px solid var(--line3);border-radius:8px;padding:6px 13px;font-size:12px;font-weight:600;color:var(--text)}\n.btn:hover{border-color:#3a4150}\n.btn.quiet{background:transparent;color:var(--mut2)}\n.btn.quiet:hover{color:var(--text)}\n.btn.accent{background:var(--acc);border-color:var(--acc);color:#1a1400}\n.btn.accent:hover{filter:brightness(1.08)}\n.btn.danger:hover{border-color:var(--red);color:var(--redsoft)}\n#banner{display:none;background:rgba(255,92,92,.08);border-bottom:1px solid rgba(255,92,92,.3);color:var(--red);font-size:11.5px;padding:7px 18px;flex:none}\n@keyframes spin{to{transform:rotate(360deg)}}\n.spin{display:inline-block;animation:spin .8s linear infinite}\n\n/* ---------- issues page ---------- */\n#tabRow{flex:none;background:var(--bg2);border-bottom:1px solid var(--line2);padding:9px 18px;display:flex;align-items:center;gap:16px}\n.cattab{display:flex;align-items:center;gap:7px;border:1px solid transparent;background:transparent;color:var(--mut);font-size:12.5px;font-weight:600;padding:6px 12px;border-radius:8px}\n.cattab:hover{color:var(--soft)}\n.cattab.on{background:var(--accsoft);color:var(--acc);border-color:var(--accbord);box-shadow:0 0 14px rgba(245,196,81,.12)}\n.cattab .n{font-family:'JetBrains Mono',monospace;font-size:10.5px;background:var(--panel);color:var(--mut);border-radius:6px;padding:1px 6px}\n.cattab.on .n{background:rgba(245,196,81,.18);color:var(--acc)}\n#repoChips{display:flex;align-items:center;gap:7px;overflow-x:auto;min-width:0}\n#repoChips .lbl{font-size:11px;color:var(--dim);font-family:'JetBrains Mono',monospace;flex:none}\n.rchip{border:1px solid var(--line2);background:var(--panel);color:var(--mut2);font-family:'JetBrains Mono',monospace;font-size:11px;font-weight:500;padding:5px 10px;border-radius:7px;white-space:nowrap;flex:none}\n.rchip:hover{border-color:#3a4150}\n.rchip.on{background:var(--accsoft);color:var(--acc);border-color:var(--accbord)}\n\n#filterRow{flex:none;background:var(--bg2);border-bottom:1px solid var(--line2);padding:8px 18px;display:flex;align-items:center;gap:10px}\n#search{display:flex;align-items:center;gap:8px;flex:1;max-width:320px;background:var(--panel);border:1px solid var(--line2);border-radius:9px;padding:7px 11px}\n#search input{flex:1;min-width:0;background:transparent;border:none;color:var(--text);font-size:12.5px}\n.drop{position:relative}\n.dropbtn{display:flex;align-items:center;gap:8px;background:var(--panel);border:1px solid var(--line2);border-radius:8px;padding:7px 11px;font-size:12px;font-weight:600;color:var(--soft)}\n.dropbtn:hover{border-color:#3a4150}\n.dropbtn .k{color:var(--mut)}\n.menu{display:none;position:absolute;top:38px;right:0;width:200px;max-height:280px;overflow-y:auto;background:var(--panel);border:1px solid var(--line3);border-radius:11px;box-shadow:0 18px 50px rgba(0,0,0,.55);z-index:30;padding:5px}\n.menu.open{display:block}\n.menu button{width:100%;text-align:left;display:flex;align-items:center;gap:9px;background:transparent;border:none;border-radius:7px;padding:8px 10px;color:var(--soft);font-size:12.5px}\n.menu button:hover{background:#171B23}\n.menu button.on{color:var(--acc)}\n.menu .sw{width:9px;height:9px;border-radius:3px;flex:none}\n\n#list{flex:1;overflow-y:auto;padding:12px 18px 16px;display:flex;flex-direction:column;gap:7px}\n.iss{background:var(--card);border:1px solid var(--line2);border-radius:11px;padding:10px 14px 10px 12px;display:flex;gap:12px;align-items:center;cursor:pointer;position:relative;overflow:hidden;transition:border-color .12s,background .12s}\n.iss::before{content:'';position:absolute;left:0;top:0;bottom:0;width:2.5px;background:var(--stripe,transparent);opacity:.85}\n.iss:hover{border-color:rgba(245,196,81,.35);background:#10131A}\n.iss:hover .tt{color:var(--acc)}\n.iss.dim{opacity:.55}\n.iss .stico{flex:none;display:flex;align-items:center;justify-content:center;width:26px;height:26px;border-radius:8px}\n.iss .ttl{font-size:13px;font-weight:600;line-height:1.35;display:flex;align-items:center;gap:7px;flex-wrap:wrap;min-width:0}\n.iss .tt{transition:color .12s}\n.iss .meta{display:flex;align-items:center;gap:8px;margin-top:3px;font-size:10.5px;color:var(--dim);flex-wrap:wrap;min-width:0}\n.iss .meta .num{font-family:'JetBrains Mono',monospace;color:var(--mut)}\n.iss .meta .rp{font-family:'JetBrains Mono',monospace}\n.iss .meta .lk{color:var(--mut);max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n.act{display:flex;align-items:center;gap:6px;flex:none;margin-left:auto}\n.btn.sm{padding:4px 10px;font-size:11px;border-radius:7px;gap:5px}\n.lblrow{display:flex;align-items:center;gap:6px;margin-top:9px;flex-wrap:wrap}\n.pill{display:inline-flex;align-items:center;gap:4px;font-size:9.5px;font-weight:500;border:1px solid;border-radius:20px;padding:1px 8px;flex:none}\n.pill .d{width:5px;height:5px;border-radius:50%}\n.state{flex:none;display:inline-flex;align-items:center;gap:5px;font-size:10.5px;font-weight:600;border-radius:6px;padding:3px 9px}\n.state .d{width:5px;height:5px;border-radius:50%}\n.iss .foot{border-top:1px solid var(--line);padding-top:12px;display:flex;align-items:center;gap:10px;cursor:default;flex-wrap:wrap}\n.linkchip{display:flex;align-items:center;gap:7px;background:var(--panel);border:1px solid var(--line2);border-radius:8px;padding:4px 10px;font-size:12px;font-weight:600;max-width:280px}\n.linkchip span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n.dimnote{font-size:11.5px;color:var(--mut)}\n.empty{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;padding:60px 20px;color:var(--dim);text-align:center}\n.empty span{font-size:13px;color:var(--mut);max-width:340px;line-height:1.5}\n#pager{flex:none;background:var(--bg2);border-top:1px solid var(--line2);padding:10px 18px;display:flex;align-items:center;gap:10px}\n#pager .info{font-size:11.5px;color:var(--mut);font-family:'JetBrains Mono',monospace}\n.pgn{min-width:30px;height:30px;border:1px solid var(--line2);background:transparent;color:var(--mut);border-radius:8px;font-size:12.5px;font-weight:600;font-family:'JetBrains Mono',monospace}\n.pgn.on{background:var(--accsoft);color:var(--acc);border-color:var(--accbord)}\n\n/* ---------- settings page ---------- */\n#pgSettings{flex:1;overflow-y:auto;padding:22px;display:none}\n#pgSettings .wrap{max-width:760px;margin:0 auto;display:flex;flex-direction:column;gap:22px}\n.shead{font-size:14px;font-weight:600;margin-bottom:3px}\n.ssub{font-size:12px;color:var(--mut);margin-bottom:12px;line-height:1.5}\n.scard{background:var(--card);border:1px solid var(--line2);border-radius:12px;overflow:hidden}\n.srow{display:flex;align-items:center;gap:14px;padding:13px 15px;border-bottom:1px solid var(--line)}\n.srow:last-child{border-bottom:none}\n.srow .g{flex:1;min-width:0}\n.srow .t{font-size:13px;font-weight:600}\n.srow .d{font-size:11.5px;color:var(--mut);margin-top:2px;line-height:1.45}\n.sw{width:38px;height:22px;border-radius:999px;border:1px solid var(--line3);background:var(--line3);position:relative;flex:none;transition:background .15s;padding:0}\n.sw i{position:absolute;top:2px;left:2px;width:16px;height:16px;border-radius:50%;background:#fff;transition:left .15s}\n.sw.on{background:var(--green)}\n.sw.on i{left:18px}\n.optrow{display:flex;gap:5px;flex-wrap:wrap;justify-content:flex-end}\n.opt{border:1px solid var(--line2);background:var(--panel);color:var(--mut2);font-size:11.5px;font-weight:600;padding:5px 11px;border-radius:7px}\n.opt:hover{border-color:#3a4150}\n.opt.on{background:var(--accsoft);color:var(--acc);border-color:var(--accbord)}\n.reponame{font-family:'JetBrains Mono',monospace;font-size:13px;font-weight:500}\n.rdot{width:8px;height:8px;border-radius:50%;background:#3a4150;flex:none}\n.rdot.on{background:var(--green)}\n.addrow{display:flex;gap:8px;align-items:center;padding:12px 15px}\n.addrow input{flex:1;background:var(--bg);border:1px solid var(--line2);border-radius:8px;color:var(--text);font-size:12.5px;padding:7px 10px;font-family:'JetBrains Mono',monospace}\n.addrow input:focus{border-color:var(--accbord)}\n.field input{width:100%;background:var(--bg);border:1px solid var(--line2);border-radius:8px;color:var(--text);font-size:12px;padding:7px 10px}\n.field input:focus{border-color:var(--accbord)}\n#triLog{max-height:150px;overflow-y:auto;font-family:'JetBrains Mono',monospace;font-size:10.5px;color:var(--mut);line-height:1.7;padding:11px 15px}\n\n/* ---------- detail drawer ---------- */\n#veil{display:none;position:fixed;inset:0;background:rgba(4,5,8,.6);z-index:40}\n#veil.open{display:block}\n#detail{display:none;position:fixed;top:0;right:0;bottom:0;width:min(560px,100%);background:var(--bg);border-left:1px solid var(--line3);z-index:41;flex-direction:column;box-shadow:-24px 0 60px rgba(0,0,0,.5)}\n#detail.open{display:flex}\n#dHead{flex:none;display:flex;align-items:center;gap:12px;padding:14px 18px;border-bottom:1px solid var(--line2)}\n#dHead .k{font-size:13.5px;font-weight:600}\n#dHead .s{font-size:11px;color:var(--dim);font-family:'JetBrains Mono',monospace;margin-top:1px}\n#dBody{flex:1;overflow-y:auto;padding:18px}\n#dBody .ttl{font-size:17px;font-weight:600;line-height:1.35}\n.dcard{background:var(--card);border:1px solid var(--line2);border-radius:11px;padding:13px 15px;margin-top:12px;font-size:12.5px;color:var(--soft);line-height:1.6;white-space:pre-wrap;word-break:break-word}\n.chead{font-size:10px;letter-spacing:.6px;color:var(--dim);text-transform:uppercase;font-weight:600;margin:18px 0 4px;font-family:'JetBrains Mono',monospace}\n.cmt{display:flex;gap:10px;margin-top:10px}\n.cmt .av{width:26px;height:26px;border-radius:7px;background:var(--panel);border:1px solid var(--line2);display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:9px;font-weight:600;color:var(--mut2);flex:none}\n.cmt .bub{flex:1;background:var(--card);border:1px solid var(--line2);border-radius:10px;padding:9px 12px;font-size:12px;color:var(--soft);line-height:1.55;white-space:pre-wrap;word-break:break-word}\n.cmt .who{font-size:11px;font-weight:600;color:var(--text)}\n.cmt .when{font-size:10px;color:var(--dim);margin-left:7px}\n#dFoot{flex:none;display:flex;align-items:center;gap:10px;padding:13px 18px;border-top:1px solid var(--line2)}\na{color:var(--acc);text-decoration:none}\n.x{width:30px;height:30px;border-radius:8px;border:1px solid var(--line2);background:transparent;color:var(--mut2);font-size:14px;display:flex;align-items:center;justify-content:center}\n.x:hover{color:var(--text);background:#171B23}\n</style></head><body>\n\n<div id=\"topbar\">\n  <svg width=\"19\" height=\"19\" viewBox=\"0 0 24 24\" fill=\"#E7E9F0\"><path d=\"M12 2C6.5 2 2 6.6 2 12.3c0 4.5 2.9 8.4 6.8 9.7.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.2-3.4-1.2-.4-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.6 2.4 1.1 3 .9.1-.7.4-1.1.6-1.4-2.2-.3-4.6-1.1-4.6-5 0-1.1.4-2 1-2.7-.1-.3-.4-1.3.1-2.7 0 0 .8-.3 2.7 1a9.3 9.3 0 015 0c1.9-1.3 2.7-1 2.7-1 .5 1.4.2 2.4.1 2.7.6.7 1 1.6 1 2.7 0 3.9-2.4 4.7-4.6 5 .4.3.7.9.7 1.9v2.8c0 .3.2.6.7.5a10.3 10.3 0 006.8-9.7C22 6.6 17.5 2 12 2z\"/></svg>\n  <span class=\"title\">GitHub Issues</span>\n  <div class=\"seg\">\n    <button id=\"modeIssues\" class=\"on\" onclick=\"setMode('issues')\">Issues</button>\n    <button id=\"modeSettings\" onclick=\"setMode('settings')\">Settings</button>\n  </div>\n  <span style=\"flex:1\"></span>\n  <span id=\"lastSync\" style=\"font-size:11.5px;color:var(--dim)\"></span>\n  <button id=\"syncBtn\" class=\"btn\" onclick=\"syncNow()\"><span id=\"syncIc\">⟳</span> Sync now</button>\n</div>\n<div id=\"banner\"></div>\n\n<div id=\"pgIssues\" style=\"flex:1;display:flex;flex-direction:column;min-height:0\">\n  <div id=\"tabRow\">\n    <div style=\"display:flex;align-items:center;gap:4px\" id=\"catTabs\"></div>\n    <span style=\"flex:1\"></span>\n    <div id=\"repoChips\"></div>\n  </div>\n  <div id=\"filterRow\">\n    <div id=\"search\">\n      <svg width=\"15\" height=\"15\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"#626B79\" stroke-width=\"1.7\" stroke-linecap=\"round\"><circle cx=\"11\" cy=\"11\" r=\"7\"/><path d=\"M20 20l-3.2-3.2\"/></svg>\n      <input id=\"q\" placeholder=\"Search issues, #number, repo, author…\" oninput=\"ui.q=this.value;ui.page=1;paintList()\">\n    </div>\n    <span style=\"flex:1\"></span>\n    <div class=\"drop\">\n      <button class=\"dropbtn\" onclick=\"toggleMenu('label',event)\"><span class=\"k\">Label:</span> <span id=\"labelLbl\">All labels</span> ▾</button>\n      <div id=\"labelMenu\" class=\"menu\"></div>\n    </div>\n    <div class=\"drop\">\n      <button class=\"dropbtn\" onclick=\"toggleMenu('sort',event)\"><span class=\"k\">Sort:</span> <span id=\"sortLbl\">Newest</span> ▾</button>\n      <div id=\"sortMenu\" class=\"menu\"></div>\n    </div>\n  </div>\n  <div id=\"list\"></div>\n  <div id=\"pager\" style=\"display:none\"></div>\n</div>\n\n<div id=\"pgSettings\">\n  <div class=\"wrap\">\n    <div>\n      <div style=\"display:flex;align-items:center;gap:9px\" class=\"shead\">Repositories\n        <span id=\"repoCount\" class=\"mono\" style=\"font-size:10.5px;color:var(--acc);background:var(--accsoft);border-radius:6px;padding:2px 8px;font-weight:500\"></span></div>\n      <div class=\"ssub\">Which repositories the sync pulls open issues from. Toggle any repo on or off; issues already tracked stay put.</div>\n      <div class=\"scard\">\n        <div id=\"repoRows\"></div>\n        <div class=\"addrow\">\n          <input id=\"newRepo\" placeholder=\"owner/repository\" spellcheck=\"false\" onkeydown=\"if(event.key==='Enter')addRepo()\">\n          <button class=\"btn\" onclick=\"addRepo()\">＋ Add</button>\n        </div>\n      </div>\n    </div>\n\n    <div>\n      <div class=\"shead\">Sync behavior</div>\n      <div class=\"ssub\" id=\"tokLine\">checking token…</div>\n      <div class=\"scard\">\n        <div class=\"srow\">\n          <div class=\"g\"><div class=\"t\">Sync frequency</div><div class=\"d\">How often new and updated issues are pulled. Runs as a schedule; Manual only syncs on the button.</div></div>\n          <div class=\"optrow\" id=\"freqOpts\"></div>\n        </div>\n        <div class=\"srow\">\n          <div class=\"g\"><div class=\"t\">Label filter</div><div class=\"d\">Only sync issues carrying one of these labels (comma-separated). Blank = all issues.</div></div>\n          <div class=\"field\" style=\"width:220px\"><input id=\"labels\" placeholder=\"bug,help wanted\" onchange=\"saveInputs()\"></div>\n        </div>\n        <div class=\"srow\">\n          <div class=\"g\"><div class=\"t\">Workdir for spawned tasks</div><div class=\"d\">Local checkout where spawned tasks run. Blank = the workspace default.</div></div>\n          <div class=\"field\" style=\"width:220px\"><input id=\"cwd\" placeholder=\"(workspace default)\" onchange=\"saveInputs()\"></div>\n        </div>\n        <div id=\"behaviorToggles\"></div>\n      </div>\n    </div>\n\n    <div>\n      <div class=\"shead\">Agent triage log</div>\n      <div class=\"ssub\">What the auto-triage agent decided, newest first. Refine its policy in this addon's Customize chat.</div>\n      <div class=\"scard\"><div id=\"triLog\"></div></div>\n    </div>\n  </div>\n</div>\n\n<div id=\"veil\" onclick=\"closeDetail()\"></div>\n<div id=\"detail\">\n  <div id=\"dHead\">\n    <svg width=\"17\" height=\"17\" viewBox=\"0 0 24 24\" fill=\"#E7E9F0\"><path d=\"M12 2C6.5 2 2 6.6 2 12.3c0 4.5 2.9 8.4 6.8 9.7.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.2-3.4-1.2-.4-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.6 2.4 1.1 3 .9.1-.7.4-1.1.6-1.4-2.2-.3-4.6-1.1-4.6-5 0-1.1.4-2 1-2.7-.1-.3-.4-1.3.1-2.7 0 0 .8-.3 2.7 1a9.3 9.3 0 015 0c1.9-1.3 2.7-1 2.7-1 .5 1.4.2 2.4.1 2.7.6.7 1 1.6 1 2.7 0 3.9-2.4 4.7-4.6 5 .4.3.7.9.7 1.9v2.8c0 .3.2.6.7.5a10.3 10.3 0 006.8-9.7C22 6.6 17.5 2 12 2z\"/></svg>\n    <div style=\"flex:1;min-width:0\"><div class=\"k\">Issue detail</div><div class=\"s\" id=\"dSub\"></div></div>\n    <a id=\"dLink\" class=\"btn\" target=\"_blank\" rel=\"noreferrer\" style=\"text-decoration:none\">Open on GitHub ↗</a>\n    <button class=\"x\" onclick=\"closeDetail()\">✕</button>\n  </div>\n  <div id=\"dBody\"></div>\n  <div id=\"dFoot\"></div>\n</div>\n\n<script>\n'use strict'\n// ---------------- host bridge ----------------\nlet seq = 0\nconst pending = {}\nfunction call(method, ...args){\n  return new Promise((resolve, reject) => {\n    const callId = 'c' + (++seq)\n    pending[callId] = { resolve, reject }\n    parent.postMessage({ type: 'yaam:call', callId, method, args }, '*')\n  })\n}\nlet snap = null // pushed app snapshot (~3s) — task columns for linked issues\nwindow.addEventListener('message', e => {\n  const d = e.data\n  if (d && d.type === 'yaam:result' && pending[d.callId]) {\n    d.error ? pending[d.callId].reject(new Error(d.error)) : pending[d.callId].resolve(d.result)\n    delete pending[d.callId]\n  }\n  if (d && d.type === 'yaam:state') { snap = d.state || snap; refresh() }\n})\nfunction banner(msg){\n  const b = document.getElementById('banner')\n  if (!msg) { b.style.display = 'none'; return }\n  b.textContent = /permission \"/.test(msg) ? msg + ' — grant it in the Addons view, then retry.' : msg\n  b.style.display = 'block'\n  setTimeout(() => { b.style.display = 'none' }, 7000)\n}\nconst esc = t => String(t == null ? '' : t).replace(/[&<>\"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]))\nconst ago = t => { const s = (Date.now() - t) / 1000; return s < 90 ? 'just now' : s < 3600 ? Math.round(s / 60) + 'm ago' : s < 86400 ? Math.round(s / 3600) + 'h ago' : Math.round(s / 86400) + 'd ago' }\nfunction lblColor(name){\n  let h = 0\n  for (let i = 0; i < name.length; i++) h = (h * 31 + name.charCodeAt(i)) % 360\n  return `hsl(${h},55%,68%)`\n}\nconst rgba = (h, s, l, a) => `hsla(${h},${s}%,${l}%,${a})`\n\n// ---------------- state ----------------\nlet cfg = {}, issues = [], tokenSet = false\nconst ui = { mode: 'issues', tab: 'inbox', repo: 'all', q: '', label: 'all', sort: 'newest', page: 1, menu: null, detail: null }\nconst PAGE = 8\nconst SCHED = 'github-issues-sync'\nconst COLSTAT = {\n  backlog: { l: 'Queued', c: '#6B7280' }, progress: { l: 'In progress', c: '#3DDC97' },\n  review: { l: 'In review', c: '#FFB020' }, done: { l: 'Done', c: '#A371F7' }, failed: { l: 'Failed', c: '#FF5C5C' },\n}\n\n// v1 → v2 migration: single owner/repo + queue/seen become the repos list and\n// the unified issue store. Runs in the view and (same logic) in the sync hook.\nfunction migrate(){\n  let dirty = false\n  if (!Array.isArray(cfg.repos)) {\n    cfg.repos = (cfg.owner && cfg.repo) ? [{ name: cfg.owner + '/' + cfg.repo, on: true }] : []\n    cfg.legacy = cfg.owner && cfg.repo ? cfg.owner + '/' + cfg.repo : ''\n    cfg.freq = cfg.schedOn ? (cfg.interval || '*/30 * * * *') : 'manual'\n    dirty = true\n  }\n  return dirty\n}\nasync function migrateIssues(){\n  const queue = (await call('storage.get', 'queue')) || []\n  if (!queue.length) return\n  const repo = cfg.legacy || (cfg.repos[0] && cfg.repos[0].name) || ''\n  for (const q of queue) {\n    const key = repo + '#' + q.number\n    if (!issues.some(i => i.key === key)) {\n      issues.push({ key, number: q.number, repo, title: q.title, body: q.body || '', labels: q.labels || [],\n        url: q.url, author: '', comments: 0, createdAt: q.at, at: q.at, state: 'inbox', taskId: null })\n    }\n  }\n  await call('storage.set', 'issues', issues)\n  await call('storage.remove', 'queue')\n}\n\nasync function load(){\n  try {\n    cfg = (await call('storage.get', 'config')) || {}\n    issues = (await call('storage.get', 'issues')) || []\n    if (migrate()) await call('storage.set', 'config', cfg)\n    await migrateIssues()\n  } catch (e) { banner(e.message) }\n  try {\n    const secrets = await call('secrets.list')\n    tokenSet = !!secrets.find(s => s.name === 'GITHUB_TOKEN' && s.set)\n  } catch (e) { /* secrets scope not granted */ }\n  paint()\n  refresh()\n}\nconst saveCfg = () => call('storage.set', 'config', cfg).catch(e => banner(e.message))\nconst saveIssues = () => call('storage.set', 'issues', issues.slice(-300)).catch(e => banner(e.message))\n\n// ---------------- derived ----------------\nfunction filtered(){\n  const q = ui.q.trim().toLowerCase()\n  return issues.filter(i => ui.repo === 'all' || i.repo === ui.repo)\n    .filter(i => !q || i.title.toLowerCase().includes(q) || ('#' + i.number).includes(q) || i.repo.toLowerCase().includes(q) || (i.author || '').toLowerCase().includes(q))\n    .filter(i => ui.label === 'all' || (i.labels || []).includes(ui.label))\n}\nfunction sorted(list){\n  const by = {\n    newest: (a, b) => (b.createdAt || b.at) - (a.createdAt || a.at),\n    oldest: (a, b) => (a.createdAt || a.at) - (b.createdAt || b.at),\n    comments: (a, b) => (b.comments || 0) - (a.comments || 0),\n    number: (a, b) => b.number - a.number,\n  }\n  return list.slice().sort(by[ui.sort] || by.newest)\n}\nconst taskOf = i => (i.taskId && snap && snap.tasks) ? snap.tasks.find(t => t.id === i.taskId) : null\n\n// ---------------- issue actions ----------------\nasync function spawnTask(key, btn){\n  const it = issues.find(i => i.key === key); if (!it) return\n  if (btn) { btn.disabled = true; btn.textContent = 'Spawning…' }\n  try {\n    const taskId = await call('tasks.add', ('#' + it.number + ' · ' + it.title).slice(0, 120), 'backlog', {\n      description: (it.body || '') + '\\n\\n' + it.url,\n      cwd: cfg.cwd || undefined,\n    })\n    it.state = 'synced'; it.taskId = taskId\n    await saveIssues()\n    if (cfg.startOnSpawn) await call('tasks.start', taskId)\n    await call('flash', 'task spawned for #' + it.number)\n  } catch (e) { banner(e.message) }\n  refresh(); refreshDetailFoot(key)\n}\nasync function setIssueState(key, state, note){\n  const it = issues.find(i => i.key === key); if (!it) return\n  it.state = state\n  if (state !== 'synced') it.taskId = null\n  await saveIssues()\n  if (note) call('flash', note)\n  refresh(); refreshDetailFoot(key)\n}\n// keep an open drawer's action row in step after spawn/ignore/restore\nfunction refreshDetailFoot(key){\n  if (ui.detail !== key) return\n  const it = issues.find(i => i.key === key); if (!it) return\n  const foot = document.getElementById('dFoot')\n  foot.innerHTML = ''\n  fillFoot(foot, it)\n}\nasync function openInBoard(taskId){\n  try { await call('focusTask', taskId) } catch (e) { banner(e.message) }\n}\nasync function startLinked(taskId){\n  try { await call('tasks.start', taskId); await call('flash', 'task started') } catch (e) { banner(e.message) }\n}\n\n// ---------------- sync ----------------\nfunction ghHeaders(){\n  const headers = { accept: 'application/vnd.github+json' }\n  if (tokenSet) headers.authorization = 'Bearer {{secret:GITHUB_TOKEN}}'\n  return headers\n}\nasync function syncNow(){\n  const repos = (cfg.repos || []).filter(r => r.on)\n  if (!repos.length) { banner('add a repository in Settings first'); setMode('settings'); return }\n  const btn = document.getElementById('syncBtn'), ic = document.getElementById('syncIc')\n  btn.disabled = true; ic.className = 'spin'\n  let found = 0\n  const fresh = []\n  try {\n    const seen = (await call('storage.get', 'seen')) || []\n    for (const r of repos) {\n      const labels = cfg.labels ? '&labels=' + encodeURIComponent(cfg.labels) : ''\n      const res = await call('http.request', 'GET',\n        'https://api.github.com/repos/' + r.name + '/issues?state=open&per_page=50&sort=created&direction=desc' + labels,\n        { headers: ghHeaders() })\n      if (res.status === 404) { banner(r.name + ': not found (private without a token?)'); continue }\n      if (res.status !== 200) { banner(r.name + ': GitHub replied HTTP ' + res.status); continue }\n      for (const raw of JSON.parse(res.text)) {\n        if (raw.pull_request) continue\n        const key = r.name + '#' + raw.number\n        const ex = issues.find(i => i.key === key)\n        if (ex) { // refresh live fields on issues we already track\n          ex.title = raw.title; ex.comments = raw.comments || 0\n          ex.labels = (raw.labels || []).map(l => l.name)\n          continue\n        }\n        if (r.name === cfg.legacy && seen.includes(raw.number)) continue // handled in v1\n        const rec = { key, number: raw.number, repo: r.name, title: raw.title,\n          body: (raw.body || '').slice(0, 4000), labels: (raw.labels || []).map(l => l.name),\n          url: raw.html_url, author: (raw.user && raw.user.login) || '',\n          comments: raw.comments || 0, createdAt: Date.parse(raw.created_at) || Date.now(),\n          at: Date.now(), state: 'inbox', taskId: null }\n        issues.push(rec); fresh.push(rec); found++\n      }\n    }\n    await saveIssues()\n    await call('storage.set', 'lastSync', { at: Date.now(), found })\n    await call('flash', found ? found + ' new issue(s) in the inbox' : 'no new issues')\n    if (fresh.length && cfg.auto) {\n      await call('agent.wake', 'New GitHub issues arrived — triage them per your instructions:\\n\\n'\n        + JSON.stringify(fresh.map(f => ({ key: f.key, number: f.number, repo: f.repo, title: f.title, labels: f.labels, body: (f.body || '').slice(0, 400) })), null, 2))\n    }\n  } catch (e) { banner(e.message) }\n  btn.disabled = false; ic.className = ''\n  refresh()\n}\n\n// ---------------- settings actions ----------------\nasync function addRepo(){\n  const el = document.getElementById('newRepo')\n  const name = el.value.trim().replace(/^https:\\/\\/github\\.com\\//, '').replace(/\\/+$/, '')\n  if (!/^[\\w.-]+\\/[\\w.-]+$/.test(name)) { banner('use the owner/repository form, e.g. anthropics/claude-code'); return }\n  if ((cfg.repos || []).some(r => r.name.toLowerCase() === name.toLowerCase())) { banner('already in the list'); return }\n  cfg.repos = (cfg.repos || []).concat([{ name, on: true }])\n  el.value = ''\n  await saveCfg(); paint()\n}\nasync function toggleRepo(name){\n  const r = (cfg.repos || []).find(x => x.name === name); if (!r) return\n  r.on = !r.on\n  await saveCfg(); paint()\n}\nasync function removeRepo(name){\n  cfg.repos = (cfg.repos || []).filter(x => x.name !== name)\n  if (ui.repo === name) ui.repo = 'all'\n  await saveCfg(); paint()\n}\nasync function setFreq(v){\n  cfg.freq = v\n  try {\n    await call('schedules.remove', SCHED).catch(() => {})\n    if (v !== 'manual') await call('schedules.add', { name: SCHED, schedule: v })\n  } catch (e) { banner(e.message); return }\n  await saveCfg(); paint()\n}\nasync function toggleCfg(k){\n  cfg[k] = !cfg[k]\n  await saveCfg(); paint()\n}\nasync function saveInputs(){\n  cfg.labels = document.getElementById('labels').value.trim()\n  cfg.cwd = document.getElementById('cwd').value.trim()\n  await saveCfg()\n}\n\n// ---------------- render ----------------\nfunction setMode(m){ ui.mode = m; ui.menu = null; paint() }\nfunction toggleMenu(m, ev){ if (ev) ev.stopPropagation(); ui.menu = ui.menu === m ? null : m; paintMenus() }\ndocument.addEventListener('click', () => { if (ui.menu) { ui.menu = null; paintMenus() } })\ndocument.addEventListener('keydown', ev => { if (ev.key === 'Escape') { ui.menu = null; closeDetail(); paintMenus() } })\n\nfunction paint(){\n  document.getElementById('modeIssues').className = ui.mode === 'issues' ? 'on' : ''\n  document.getElementById('modeSettings').className = ui.mode === 'settings' ? 'on' : ''\n  document.getElementById('pgIssues').style.display = ui.mode === 'issues' ? 'flex' : 'none'\n  document.getElementById('pgSettings').style.display = ui.mode === 'settings' ? 'block' : 'none'\n  if (ui.mode === 'settings') paintSettings()\n  else { paintTabs(); paintMenus(); paintList() }\n}\nasync function refresh(){\n  try {\n    const last = await call('storage.get', 'lastSync')\n    document.getElementById('lastSync').textContent = last ? 'Last synced ' + ago(last.at) : 'Never synced'\n    issues = (await call('storage.get', 'issues')) || issues\n  } catch (e) { return }\n  paint()\n}\n\nfunction paintTabs(){\n  const base = filtered()\n  const counts = { inbox: 0, synced: 0, ignored: 0, archived: 0 }\n  for (const i of base) counts[i.state] = (counts[i.state] || 0) + 1\n  const el = document.getElementById('catTabs')\n  el.innerHTML = ''\n  for (const t of [['inbox', 'Inbox'], ['synced', 'Synced'], ['ignored', 'Ignored'], ['archived', 'Archived']]) {\n    const b = document.createElement('button')\n    b.className = 'cattab' + (ui.tab === t[0] ? ' on' : '')\n    b.innerHTML = esc(t[1]) + ' <span class=\"n\">' + (counts[t[0]] || 0) + '</span>'\n    b.onclick = () => { ui.tab = t[0]; ui.page = 1; paint() }\n    el.appendChild(b)\n  }\n  const rc = document.getElementById('repoChips')\n  rc.innerHTML = '<span class=\"lbl\">repo</span>'\n  const names = ['all'].concat((cfg.repos || []).filter(r => r.on).map(r => r.name))\n  for (const n of names) {\n    const b = document.createElement('button')\n    b.className = 'rchip' + (ui.repo === n ? ' on' : '')\n    b.textContent = n === 'all' ? 'All repos' : n.split('/')[1]\n    b.title = n\n    b.onclick = () => { ui.repo = n; ui.page = 1; paint() }\n    rc.appendChild(b)\n  }\n}\n\nfunction paintMenus(){\n  const labels = {}\n  for (const i of issues) for (const l of (i.labels || [])) labels[l] = 1\n  const lm = document.getElementById('labelMenu')\n  lm.className = 'menu' + (ui.menu === 'label' ? ' open' : '')\n  lm.innerHTML = ''\n  for (const name of ['all'].concat(Object.keys(labels).sort())) {\n    const b = document.createElement('button')\n    b.className = ui.label === name ? 'on' : ''\n    const c = name === 'all' ? '#3a4150' : lblColor(name)\n    b.innerHTML = '<span class=\"sw\" style=\"background:' + c + '\"></span><span style=\"flex:1\">' + (name === 'all' ? 'All labels' : esc(name)) + '</span>' + (ui.label === name ? '✓' : '')\n    b.onclick = ev => { ev.stopPropagation(); ui.label = name; ui.menu = null; ui.page = 1; paint() }\n    lm.appendChild(b)\n  }\n  document.getElementById('labelLbl').textContent = ui.label === 'all' ? 'All labels' : ui.label\n  const sm = document.getElementById('sortMenu')\n  sm.className = 'menu' + (ui.menu === 'sort' ? ' open' : '')\n  sm.innerHTML = ''\n  for (const s of [['newest', 'Newest'], ['oldest', 'Oldest'], ['comments', 'Most comments'], ['number', 'Issue number']]) {\n    const b = document.createElement('button')\n    b.className = ui.sort === s[0] ? 'on' : ''\n    b.innerHTML = '<span style=\"flex:1\">' + s[1] + '</span>' + (ui.sort === s[0] ? '✓' : '')\n    b.onclick = ev => { ev.stopPropagation(); ui.sort = s[0]; ui.menu = null; ui.page = 1; paint() }\n    sm.appendChild(b)\n  }\n  document.getElementById('sortLbl').textContent = ({ newest: 'Newest', oldest: 'Oldest', comments: 'Most comments', number: 'Issue number' })[ui.sort]\n}\n\nfunction statePill(it){\n  const closed = !!it.closed\n  const c = closed ? 'var(--purple)' : 'var(--green)'\n  return '<span class=\"state\" style=\"color:' + c + ';background:' + (closed ? 'rgba(163,113,247,.14)' : 'rgba(61,220,151,.14)') + '\"><span class=\"d\" style=\"background:' + c + '\"></span>' + (closed ? 'Closed' : 'Open') + '</span>'\n}\nfunction labelPills(it){\n  return (it.labels || []).map(l => {\n    const c = lblColor(l)\n    return '<span class=\"pill\" style=\"color:' + c + ';border-color:color-mix(in srgb,' + c + ' 30%,transparent);background:color-mix(in srgb,' + c + ' 12%,transparent)\"><span class=\"d\" style=\"background:' + c + '\"></span>' + esc(l) + '</span>'\n  }).join('')\n}\n\n// GitHub-style state glyph: green open circle-dot, purple closed check\nfunction stateIcon(it){\n  const closed = !!it.closed\n  const c = closed ? '#A371F7' : '#3DDC97'\n  const glyph = closed\n    ? '<svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"' + c + '\" stroke-width=\"1.9\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><circle cx=\"12\" cy=\"12\" r=\"8.5\"/><path d=\"M8.5 12.2l2.4 2.4 4.4-4.8\"/></svg>'\n    : '<svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"' + c + '\" stroke-width=\"1.9\"><circle cx=\"12\" cy=\"12\" r=\"8.5\"/><circle cx=\"12\" cy=\"12\" r=\"2.4\" fill=\"' + c + '\" stroke=\"none\"/></svg>'\n  return '<span class=\"stico\" style=\"background:color-mix(in srgb,' + c + ' 10%,transparent)\">' + glyph + '</span>'\n}\nfunction issueCard(it){\n  const d = document.createElement('div')\n  d.className = 'iss' + (it.state === 'ignored' || it.state === 'archived' ? ' dim' : '')\n  d.style.setProperty('--stripe', it.closed ? '#A371F7' : it.state === 'ignored' || it.state === 'archived' ? '#3a4150' : '#3DDC97')\n  d.onclick = () => openDetail(it.key)\n  const t = it.state === 'synced' ? taskOf(it) : null\n  const st = t ? (COLSTAT[t.col] || COLSTAT.backlog) : null\n  const meta = '<span class=\"num\">#' + it.number + '</span><span class=\"rp\">' + esc(it.repo.split('/')[1] || it.repo) + '</span>'\n    + '<span>' + (it.author ? esc(it.author) + ' · ' : '') + ago(it.createdAt || it.at) + '</span>'\n    + ((it.comments || 0) ? '<span>💬 ' + it.comments + '</span>' : '')\n    + (t ? '<span class=\"lk\">→ ' + esc(t.title) + '</span>'\n      : it.state === 'synced' ? '<span class=\"lk\">→ linked task is gone — unlink to triage again</span>' : '')\n    + (it.state === 'ignored' ? '<span>· ignored</span>' : it.state === 'archived' ? '<span>· archived</span>' : '')\n  d.innerHTML = stateIcon(it)\n    + '<div style=\"flex:1;min-width:0\"><div class=\"ttl\"><span class=\"tt\">' + esc(it.title) + '</span>' + labelPills(it) + '</div>'\n    + '<div class=\"meta\">' + meta + '</div></div>'\n  const act = document.createElement('div')\n  act.className = 'act'\n  act.onclick = ev => ev.stopPropagation()\n  const btn = (cls, label, fn, title) => { const b = document.createElement('button'); b.className = cls + ' sm'; b.innerHTML = label; b.onclick = fn; if (title) b.title = title; return b }\n  if (it.state === 'inbox') {\n    act.appendChild(btn('btn accent', '⇥ Spawn', ev => spawnTask(it.key, ev.currentTarget), 'Create a linked board task'))\n    act.appendChild(btn('btn quiet', 'Ignore', () => setIssueState(it.key, 'ignored', 'issue ignored')))\n    act.appendChild(btn('btn quiet', 'Archive', () => setIssueState(it.key, 'archived', 'issue archived')))\n  } else if (it.state === 'synced') {\n    if (st) {\n      const chip = document.createElement('span')\n      chip.className = 'state'\n      chip.style.color = st.c\n      chip.style.background = 'color-mix(in srgb,' + st.c + ' 14%,transparent)'\n      chip.innerHTML = '<span class=\"d\" style=\"background:' + st.c + '\"></span>' + st.l\n      act.appendChild(chip)\n      if (t.col === 'backlog') act.appendChild(btn('btn quiet', '▶', () => startLinked(it.taskId), 'Start the linked task'))\n      act.appendChild(btn('btn quiet', 'Board', () => openInBoard(it.taskId), 'Open the task on the board'))\n    }\n    act.appendChild(btn('btn quiet danger', 'Unlink', () => setIssueState(it.key, 'inbox', 'unlinked — back in the inbox')))\n  } else {\n    act.appendChild(btn('btn quiet', '<span style=\"color:var(--acc)\">Restore</span>', () => setIssueState(it.key, 'inbox', 'restored to inbox')))\n  }\n  d.appendChild(act)\n  return d\n}\n\nfunction fillFoot(foot, it){\n  const btn = (cls, label, fn) => { const b = document.createElement('button'); b.className = cls; b.innerHTML = label; b.onclick = fn; return b }\n  const spacer = () => { const s = document.createElement('span'); s.style.flex = '1'; return s }\n  if (it.state === 'inbox') {\n    foot.appendChild(btn('btn accent', '⇥ Spawn task', ev => spawnTask(it.key, ev.currentTarget)))\n    foot.appendChild(spacer())\n    foot.appendChild(btn('btn quiet', 'Ignore', () => setIssueState(it.key, 'ignored', 'issue ignored')))\n    foot.appendChild(btn('btn quiet', 'Archive', () => setIssueState(it.key, 'archived', 'issue archived')))\n  } else if (it.state === 'synced') {\n    const t = taskOf(it)\n    const wrap = document.createElement('div')\n    wrap.style.cssText = 'display:flex;align-items:center;gap:9px;min-width:0;flex-wrap:wrap'\n    if (t) {\n      const st = COLSTAT[t.col] || COLSTAT.backlog\n      wrap.innerHTML = '<span style=\"font-size:11px;color:var(--dim);font-family:\\'JetBrains Mono\\',monospace\">linked task</span>'\n        + '<span class=\"linkchip\"><span>' + esc(t.title) + '</span></span>'\n        + '<span class=\"state\" style=\"color:' + st.c + ';background:color-mix(in srgb,' + st.c + ' 14%,transparent)\"><span class=\"d\" style=\"background:' + st.c + '\"></span>' + st.l + '</span>'\n    } else {\n      wrap.innerHTML = '<span class=\"dimnote\">linked task is gone — unlink to triage again</span>'\n    }\n    foot.appendChild(wrap)\n    foot.appendChild(spacer())\n    if (t) {\n      if (t.col === 'backlog') foot.appendChild(btn('btn quiet', '▶ Start', () => startLinked(it.taskId)))\n      foot.appendChild(btn('btn quiet', 'Open in board', () => openInBoard(it.taskId)))\n    }\n    foot.appendChild(btn('btn quiet danger', 'Unlink', () => setIssueState(it.key, 'inbox', 'unlinked — back in the inbox')))\n  } else {\n    const note = document.createElement('span')\n    note.className = 'dimnote'\n    note.textContent = it.state === 'ignored' ? 'Ignored — hidden from the inbox' : 'Archived — no longer tracked'\n    foot.appendChild(note)\n    foot.appendChild(spacer())\n    foot.appendChild(btn('btn quiet', '<span style=\"color:var(--acc)\">Restore</span>', () => setIssueState(it.key, 'inbox', 'restored to inbox')))\n  }\n}\n\nfunction paintList(){\n  const list = sorted(filtered().filter(i => i.state === ui.tab))\n  const el = document.getElementById('list')\n  el.innerHTML = ''\n  const pages = Math.max(1, Math.ceil(list.length / PAGE))\n  ui.page = Math.min(Math.max(1, ui.page), pages)\n  for (const it of list.slice((ui.page - 1) * PAGE, ui.page * PAGE)) el.appendChild(issueCard(it))\n  if (!list.length) {\n    const msgs = {\n      inbox: (cfg.repos || []).some(r => r.on) ? 'No new issues to triage. Everything in scope is synced or filed away.' : 'Add a repository in Settings, then hit ⟳ Sync now.',\n      synced: 'No issues are linked to a task yet. Spawn one from the Inbox.',\n      ignored: 'Nothing ignored.', archived: 'Nothing archived.',\n    }\n    el.innerHTML = '<div class=\"empty\"><svg width=\"38\" height=\"38\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"#3a4150\" stroke-width=\"1.5\"><circle cx=\"12\" cy=\"12\" r=\"9\"/><circle cx=\"12\" cy=\"12\" r=\"3\" fill=\"#3a4150\" stroke=\"none\"/></svg><span>' + msgs[ui.tab] + '</span></div>'\n  }\n  const pager = document.getElementById('pager')\n  pager.style.display = pages > 1 ? 'flex' : 'none'\n  if (pages > 1) {\n    pager.innerHTML = ''\n    const info = document.createElement('span')\n    info.className = 'info'\n    info.textContent = ((ui.page - 1) * PAGE + 1) + '–' + Math.min(ui.page * PAGE, list.length) + ' of ' + list.length\n    pager.appendChild(info)\n    const sp = document.createElement('span'); sp.style.flex = '1'; pager.appendChild(sp)\n    for (let p = 1; p <= pages; p++) {\n      const b = document.createElement('button')\n      b.className = 'pgn' + (p === ui.page ? ' on' : '')\n      b.textContent = p\n      b.onclick = () => { ui.page = p; paintList() }\n      pager.appendChild(b)\n    }\n  }\n}\n\nasync function paintSettings(){\n  const rows = document.getElementById('repoRows')\n  rows.innerHTML = ''\n  const repos = cfg.repos || []\n  document.getElementById('repoCount').textContent = repos.filter(r => r.on).length + ' syncing'\n  for (const r of repos) {\n    const open = issues.filter(i => i.repo === r.name && i.state !== 'archived').length\n    const row = document.createElement('div')\n    row.className = 'srow'\n    row.innerHTML = '<span class=\"rdot' + (r.on ? ' on' : '') + '\"></span>'\n      + '<span class=\"reponame\" style=\"flex:1;color:' + (r.on ? 'var(--text)' : 'var(--mut)') + '\">' + esc(r.name) + '</span>'\n      + '<span style=\"font-size:11px;color:var(--dim);font-family:\\'JetBrains Mono\\',monospace\">' + open + ' tracked</span>'\n    const sw = document.createElement('button')\n    sw.className = 'sw' + (r.on ? ' on' : ''); sw.innerHTML = '<i></i>'\n    sw.onclick = () => toggleRepo(r.name)\n    const rm = document.createElement('button')\n    rm.className = 'btn quiet danger'; rm.textContent = '✕'; rm.title = 'Remove repository'\n    rm.onclick = () => removeRepo(r.name)\n    row.appendChild(sw); row.appendChild(rm)\n    rows.appendChild(row)\n  }\n  if (!repos.length) rows.innerHTML = '<div class=\"srow\"><span class=\"dimnote\">No repositories yet — add one below to start syncing.</span></div>'\n\n  document.getElementById('tokLine').innerHTML = tokenSet\n    ? '<span style=\"color:var(--green)\">●</span> GITHUB_TOKEN is set — private repos sync, and closing issues on Done works.'\n    : '<span style=\"color:var(--amber)\">●</span> No token — public repos only; set GITHUB_TOKEN in the Addons view to sync private repos or close issues.'\n\n  const freqs = [['manual', 'Manual'], ['*/15 * * * *', 'Every 15 min'], ['*/30 * * * *', 'Every 30 min'], ['0 * * * *', 'Hourly']]\n  const fo = document.getElementById('freqOpts')\n  fo.innerHTML = ''\n  for (const f of freqs) {\n    const b = document.createElement('button')\n    b.className = 'opt' + ((cfg.freq || 'manual') === f[0] ? ' on' : '')\n    b.textContent = f[1]\n    b.onclick = () => setFreq(f[0])\n    fo.appendChild(b)\n  }\n  document.getElementById('labels').value = cfg.labels || ''\n  document.getElementById('cwd').value = cfg.cwd || ''\n\n  const toggles = [\n    ['startOnSpawn', 'Start spawned tasks immediately', 'Spawn task launches the one-shot session right away instead of parking the task in Backlog.'],\n    ['auto', 'Auto-triage new issues', \"The addon's agent reviews each newly-synced batch and files tasks itself. Refine its policy in the Customize chat.\"],\n    ['autostart', 'Agent may also start tasks', 'Allow the triage agent to launch the tasks it files, not just create them.'],\n    ['autoClose', 'Close the issue when its task is done', 'When a linked task reaches Done, the issue is closed on GitHub (needs a token with repo scope).'],\n  ]\n  const bt = document.getElementById('behaviorToggles')\n  bt.innerHTML = ''\n  for (const t of toggles) {\n    const row = document.createElement('div')\n    row.className = 'srow'\n    row.innerHTML = '<div class=\"g\"><div class=\"t\">' + t[1] + '</div><div class=\"d\">' + t[2] + '</div></div>'\n    const sw = document.createElement('button')\n    sw.className = 'sw' + (cfg[t[0]] ? ' on' : ''); sw.innerHTML = '<i></i>'\n    sw.onclick = () => toggleCfg(t[0])\n    row.appendChild(sw)\n    bt.appendChild(row)\n  }\n\n  let tri = []\n  try { tri = (await call('storage.get', 'triageLog')) || [] } catch (e) { /* storage denied */ }\n  document.getElementById('triLog').innerHTML = tri.length\n    ? tri.map(t => '<div>' + esc(t) + '</div>').join('')\n    : '<span class=\"dimnote\">the agent has not triaged anything yet — turn on auto-triage above</span>'\n}\n\n// ---------------- detail drawer ----------------\nasync function openDetail(key){\n  const it = issues.find(i => i.key === key); if (!it) return\n  ui.detail = key\n  document.getElementById('veil').className = 'open'\n  document.getElementById('detail').className = 'open'\n  document.getElementById('dSub').textContent = it.repo + ' · #' + it.number\n  document.getElementById('dLink').href = it.url\n  const body = document.getElementById('dBody')\n  body.innerHTML = '<div class=\"ttl\">' + esc(it.title) + '</div>'\n    + '<div style=\"display:flex;align-items:center;gap:9px;margin-top:10px;flex-wrap:wrap\">' + statePill(it)\n    + '<span style=\"font-size:11.5px;color:var(--mut)\">' + (it.author ? 'opened by ' + esc(it.author) + ' · ' : '') + ago(it.createdAt || it.at) + ' · 💬 ' + (it.comments || 0) + '</span></div>'\n    + '<div class=\"lblrow\">' + labelPills(it) + '</div>'\n    + '<div class=\"dcard\">' + (esc(it.body) || '<i>No description provided.</i>') + '</div>'\n    + '<div id=\"dThread\"></div>'\n  const foot = document.getElementById('dFoot')\n  foot.innerHTML = ''\n  fillFoot(foot, it)\n  // lazy-load the comment thread straight from GitHub\n  const th = document.getElementById('dThread')\n  if (it.comments > 0) {\n    th.innerHTML = '<div class=\"chead\">Comments</div><div class=\"dimnote\">loading…</div>'\n    try {\n      const res = await call('http.request', 'GET',\n        'https://api.github.com/repos/' + it.repo + '/issues/' + it.number + '/comments?per_page=10',\n        { headers: ghHeaders() })\n      if (ui.detail !== key) return\n      if (res.status !== 200) throw new Error('HTTP ' + res.status)\n      const cmts = JSON.parse(res.text)\n      th.innerHTML = '<div class=\"chead\">' + cmts.length + ' comment(s)</div>' + cmts.map(c => {\n        const who = (c.user && c.user.login) || '?'\n        return '<div class=\"cmt\"><span class=\"av\">' + esc(who.slice(0, 2).toUpperCase()) + '</span>'\n          + '<div class=\"bub\"><span class=\"who\">' + esc(who) + '</span><span class=\"when\">' + ago(Date.parse(c.created_at) || Date.now()) + '</span>'\n          + '<div style=\"margin-top:4px\">' + esc((c.body || '').slice(0, 2000)) + '</div></div></div>'\n      }).join('')\n    } catch (e) { th.innerHTML = '<div class=\"chead\">Comments</div><div class=\"dimnote\">could not load comments — ' + esc(e.message) + '</div>' }\n  } else th.innerHTML = ''\n}\nfunction closeDetail(){\n  ui.detail = null\n  document.getElementById('veil').className = ''\n  document.getElementById('detail').className = ''\n}\n\nload()\nparent.postMessage({ type: 'yaam:getState' }, '*')\n</script></body></html>\n"
}
