QA Gate is installed: every board task that reaches the review column automatically gets an independent auditor session that re-verifies its acceptance criteria and posts "QA VERDICT: pass/fail" to the task chat; failing tasks bounce back to In progress. Before telling the user a reviewed task is ready, check the verdict with addon_qa_history. To audit a specific task on demand, call addon_qa_audit_task with its task id.
