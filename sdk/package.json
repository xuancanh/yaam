{
  "name": "yaam-sdk",
  "private": true,
  "description": "npm workspace for the YAAM addon developer toolkit (@yaam/addon-sdk, CLI, scaffolder).",
  "workspaces": [
    "addon-sdk",
    "yaam-addon",
    "create-yaam-addon"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
